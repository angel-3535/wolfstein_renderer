#pragma once

#include <raylib.h>
typedef int i32;
typedef float f32;
typedef double f64;

typedef Vector2 vec2f;

typedef struct {
  f64 x;
  f64 y;
} vec2;

typedef struct {
  i32 x;
  i32 y;
} ivec2;
